import { PlaceholderScreen } from "@/components/shell/placeholder-screen";

// Settings — language, theme, notification prefs, account/LINE binding, PDPA links.
export default function SettingsPage() {
  return <PlaceholderScreen th="ตั้งค่า" en="Settings" phase="PHASE 2" />;
}
