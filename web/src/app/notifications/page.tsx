import { PlaceholderScreen } from "@/components/shell/placeholder-screen";

// Notifications — report status, lost-found claim/match, activities, announcements.
export default function NotificationsPage() {
  return <PlaceholderScreen th="การแจ้งเตือน" en="Notifications" phase="PHASE 2" />;
}
