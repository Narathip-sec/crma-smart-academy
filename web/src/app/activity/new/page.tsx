import { PlaceholderScreen } from "@/components/shell/placeholder-screen";

// Create Activity (Meetup-style) — cadet-authored events with moderation states.
export default function CreateActivityPage() {
  return <PlaceholderScreen th="สร้างกิจกรรม" en="Create Activity" phase="PHASE 2" />;
}
