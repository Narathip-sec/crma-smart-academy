# CRMA Smart Academy — สรุปสถานะ + สิ่งที่เหลือ (2026-07-16)

**นำเสนอจริง:** 2026-08-10 (เหลือ 25 วัน)
เอกสารนี้สรุป 3 ก้อนงานที่ต้องปิดก่อนวันนำเสนอ: **แอป**, **สไลด์**, **เอกสาร proposal**. รายละเอียด dev sprint วันต่อวันดูที่ `docs/PRESENTATION-PREP-PLAN.md` — ไฟล์นี้คือภาพรวมระดับ milestone ครอบคลุมกว่า.

---

## 1. แอป (Dev)

### ✅ เสร็จแล้ว
- UI unification ครบ 14 task, LIFF login จริง, upload auth
- Mock data ลบหมด → ต่อ DB จริงทุกจุด (home, profile, grades)
- Schema `Grade`/`Semester` + migrate + seed จริงบน Prisma Postgres
- Import ข้อมูลจริง: ตารางเรียน, ปฏิทินการศึกษา, เมนูอาหาร
- OG metadata + branded icon + share card ต่อกิจกรรม
- Dark theme ตรวจแล้ว ใช้ได้จริงบนมือถือ (iOS + Android)
- Deploy Vercel สำเร็จ, auto-deploy ทุก push
- **ทดสอบอุปกรณ์จริงรอบใหญ่ (iOS + Android) — เจอ + แก้บั๊กจริง 6 จุด:**
  1. ของหาย/ของพบ: รูปไม่ขึ้น + list/detail อ้าง field ที่ไม่มีในสคีมา (type, status "open") → badge ขึ้นดิบๆ, ปุ่มขอรับของค้าง
  2. กิจกรรม: สร้างแล้วไม่ขึ้น list (ติด moderation ไม่มี UI ให้อนุมัติ) → เปลี่ยนเป็น auto-approve
  3. กิจกรรม: การ์ดไม่มีรูป + ปุ่ม RSVP เช็ค enum ผิดค่า → RSVP ค้าง disabled ทุกกิจกรรม
  4. แจ้งซ่อม: list ไม่มีรูป/รายละเอียด/สถานที่ + เลขที่ตั๋วพิมพ์ผิด field
  5. Sweep ทั้งสคีมาแล้ว ยืนยันไม่เหลือจุดอื่นแบบเดียวกัน
  6. Settings เข้าถึงยาก → เพิ่มไอคอนบนหน้าแรกเลย (จากเดิมต้องผ่านหน้าโปรไฟล์)
- Client-side image compression ก่อนอัปโหลด (ลดขนาดได้ ~95% กับรูปกล้องมือถือ)
- **Rich Menu ยิงจริงแล้ว (2026-07-21)** — สมัคร Messaging API channel, ตั้ง token ใน `.env`, รัน `npm run rich-menu:setup` สำเร็จ ยืนยันผ่าน LINE API ว่าตั้งเป็น default rich menu จริง

### ⏳ เหลือ
- [ ] **เตรียมข้อมูลสาธิต** — ตอนนี้มีแค่ dev cadet คนเดียว + กิจกรรม/ของหาย 2-3 รายการที่ทดสอบเอง ถ้าต้องการให้ demo ดูสมจริงกว่านี้ (นักเรียนหลายคน, กิจกรรมหลากหลาย) ต้อง seed เพิ่ม
- [ ] Lighthouse mobile score — ยังไม่ได้เช็ค (ไม่ critical แต่ทำได้ถ้ามีเวลา)

**สรุป: แอปฟีเจอร์หลักใช้งานได้จริงครบแล้ว ผ่านการทดสอบอุปกรณ์จริง 2 แพลตฟอร์ม เหลือแค่ข้อมูลสาธิตให้สมจริงขึ้น**

### ❌ ตัดออก — ไม่ทำก่อนนำเสนอ
*(ไม่มีรายการในหมวดนี้ตอนนี้ — Rich Menu ที่เคยตัดออกทำเสร็จแล้ว ดูหัวข้อ "เสร็จแล้ว")*

### 🔒 Known Limitations — ตัดสินใจเลื่อนออกจากขอบเขต ไม่ใช่บั๊กที่พลาด

รายการนี้เป็นข้อจำกัดที่ทีมรับทราบและตัดสินใจไม่ทำก่อนนำเสนอ (Playbook 2, `docs/SONNET-EXECUTION-PLAYBOOK-2.md`) เพราะต้องมี infra เพิ่มหรือไม่กระทบ scope วิจัยหลัก — ใช้ประกอบสไลด์หัวข้อ "ข้อจำกัด + แผนต่อยอด":

- **Rate limiting** — API ยังไม่มีการจำกัดจำนวนคำขอต่อผู้ใช้/ต่อ IP ต้องมี infra เพิ่ม (เช่น Redis/Upstash) จึงเลื่อนไปหลังนำเสนอ
- **Session revocation / logout** — session cookie ปัจจุบันอายุ 30 วัน ไม่มีกลไก logout หรือเพิกถอน session จาก server ฝั่งเดียว (ประเมินแล้วว่าไม่ใช่ความเสี่ยงจริงในช่วง demo ระยะสั้น)
- **Activity auto-approve** — กิจกรรมที่สร้างใหม่ถูกอนุมัติอัตโนมัติทันที (ข้าม moderation queue) เพราะยังไม่มี UI/บัญชี moderator ให้อนุมัติจริง — บันทึกเป็นการตัดสินใจจงใจ (`PRESENTATION-PREP-PLAN.md` บรรทัด "กิจกรรม: สร้างแล้วไม่ขึ้น list")
- **Upload-to-record binding** — ไฟล์ที่อัปโหลดผ่าน `/api/upload` ยังไม่ผูกกับ record ที่สร้างจริงในขั้นตอนเดียว (ตรวจแค่ auth + ชนิด/ขนาดไฟล์) เป็นความเสี่ยงเล็กน้อย (orphaned blob) ไม่ใช่ช่องโหว่ security
- **Zod schema validation แบบเต็มรูปแบบ** — ตอนนี้ใช้ helper ธรรมดาใน `src/lib/validate.ts` (bounded string, date, int range, blob URL allowlist) ครอบคลุมทุก POST route ที่จำเป็นแล้ว แต่ยังไม่ใช่ schema library เต็มรูปแบบ

---

## 2. สไลด์นำเสนอ

**สถานะ: ยังไม่เริ่ม**

โครงร่างเตรียมไว้แล้วใน `docs/PRESENTATION-PREP-PLAN.md` (หัวข้อ "โครงร่างสไลด์นำเสนอ") — 9 หัวข้อ: ปก, โจทย์วิจัย, วัตถุประสงค์, สถาปัตยกรรม, UI unification before/after, feature walkthrough, ผลลัพธ์เทียบวัตถุประสงค์วิจัย, ข้อจำกัด+แผนต่อยอด, สรุป+Q&A.

พร้อมเริ่มทำจริงได้ทันทีที่บอก — มี pptx skill ช่วยสร้างจากโครงร่างนี้ตรงๆ. เนื้อหาส่วน "ผลลัพธ์" ควรอิงจาก `docs/research/RESEARCH-OBJECTIVE-ALIGNMENT-2026-06-29.md` (มีวัตถุประสงค์วิจัย 3 ข้อจากบทที่ 5 พร้อมอยู่แล้ว).

---

## 3. เอกสาร Proposal (บทที่ 1-5)

**สถานะ: เขียนครบแล้วทั้ง 5 บท**, ปรับปรุงล่าสุดแล้ว — ไม่ต้องเริ่มใหม่

พบใน `docs/research/`:
- บทที่ 1-5 (docx + pdf ครบทุกบท)
- บทที่ 1, 2 มีเวอร์ชัน **"ปรับปรุงตามบทที่ 3-5 2026-06-29"** — คือเวอร์ชันล่าสุดที่แก้วัตถุประสงค์/ขอบเขตให้ตรงกับสิ่งที่บทที่ 3-5 สรุปไว้จริง (ตัดฟีเจอร์ AI/SNA/Gamification เต็มรูปแบบออกจากขอบเขตหลัก, เหลือ 3 วัตถุประสงค์ตามบทที่ 5)
- มีรายงานฉบับสมบูรณ์ "Meetup-รายงานโครงงานวิจัยฉบับสมบูรณ์#1" (docx+pdf) ด้วย

### ⏳ เหลือ (ตามที่บันทึกไว้ใน QA notes ของไฟล์ alignment)
- [ ] เปิดไฟล์ DOCX บทที่ 1, 2 เวอร์ชันปรับปรุงใน Word/LibreOffice จริง ตรวจรูปเล่ม/การจัดหน้าขั้นสุดท้ายก่อนส่ง (ไฟล์ตรวจแค่ระดับ XML package ยังไม่เคย render ภาพจริง)
- [ ] เช็คว่าเนื้อหาบทที่ 1-2 เวอร์ชันเก่า (ที่ไม่มี "ปรับปรุงตามบทที่ 3-5") ยังใช้อ้างอิงที่ไหนอยู่ไหม — ถ้าไม่ใช้แล้วควรเก็บเป็น archive กันสับสนเวลาส่งไฟล์ผิดเวอร์ชัน
- [ ] เนื้อหาแอปจริงตอนนี้ (หลัง dev sprint นี้) อาจเกินขอบเขตที่เขียนไว้ในบทที่ 1/2 บ้างแล้ว (เช่น dark theme, image compression, rich menu) — ควรพิจารณาว่าต้องอัปเดตบทที่ 4/5 หรือไม่ก่อนส่งฉบับจริง

**สรุป: proposal พร้อม 90%+ แล้ว งานที่เหลือคือ proofread รูปเล่มจริง ไม่ใช่เขียนใหม่**

---

## ลำดับความสำคัญแนะนำ (ถ้าจะเลือกทำต่อตอนนี้)

1. **สไลด์** — เริ่มได้เลยตอนนี้ ไม่ติดอะไร
2. **Proposal proofread** — ต้องเปิด Word เองเพราะเครื่องนี้ไม่มี LibreOffice CLI render ภาพให้ดู
3. **ข้อมูลสาธิตเพิ่ม** — ทำตอนใกล้วันจริงก็ได้ ไม่เร่งด่วน
4. ~~Rich Menu~~ — ยิงจริงกับ LINE API เสร็จแล้ว
